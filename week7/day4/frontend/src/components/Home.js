import React from 'react'
import { Typography } from 'antd'

const { Title } = Typography

const Home = () => (
  <main className="myContainer">
    <Title>{'Coronavirus countries dataset'.toUpperCase()}</Title>
  </main>
)

export default Home
